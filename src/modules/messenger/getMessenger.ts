import { Messenger } from './model';

// eslint-disable-next-line @typescript-eslint/no-unused-vars
const getMessenger = async (id: Messenger['id']): Promise<Messenger | null> => null;

export default getMessenger;

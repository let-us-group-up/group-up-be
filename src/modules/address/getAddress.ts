import { Address } from './model';

// eslint-disable-next-line @typescript-eslint/no-unused-vars
const getAddress = async (id: Address['id']): Promise<Address | null> => null;

export default getAddress;
